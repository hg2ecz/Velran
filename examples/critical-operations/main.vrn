permission BillingWrite {
    role Admin
    role BillingAdmin
}

critical Payment {
    permission BillingWrite
    mfa
    transaction
    audit
}

model Payment {
    id: i64
}

#[action]
fn refund(
    ctx: ActionContext,
    db: Db,
    id: i64
) -> Result<Json, PageError> critical Payment {
    transaction db {
        audit Payment id action refund;
    }
    return Ok(json(true));
}

route refund POST "/billing/:id<i64>/refund"
    auth critical Payment
    => refund;
